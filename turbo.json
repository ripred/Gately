{
    "$schema": "https://turborepo.com/schema.json",
    "tasks": {
        "build": {
            "dependsOn": ["^build"],
            "env": ["GATELY_BASE_PATH"],
            "inputs": ["$TURBO_DEFAULT$", ".env*"],
            "outputs": ["dist/**"]
        },
        "prod": {
            "cache": false,
            "persistent": true
        },
        "lint": { "inputs": ["**/*.ts", "**/*.tsx", "eslint.config.*", "package.json"] },
        "dev": {
            "cache": false,
            "persistent": true
        },
        "test": {
            "cache": false
        }
    }
}
